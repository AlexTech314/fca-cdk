{
  "name": "fca-cdk",
  "version": "0.1.0",
  "bin": {
    "fca-cdk": "bin/fca-cdk.js"
  },
  "scripts": {
    "install:all": "node scripts/install-all.js",
    "postinstall": "node scripts/install-all.js --skip-root",
    "audit:fix:all": "npm audit fix --prefix src/packages/db && npm audit fix --prefix src/packages/seed && npm audit fix --prefix src/api && npm audit fix --prefix src/lead-gen-spa && npm audit fix --prefix src/flagship-ui/nextjs-web && npm audit fix --prefix src/lambda/bridge && npm audit fix --prefix src/lambda/seed-db && npm audit fix --prefix src/lambda/start-places && npm audit fix --prefix src/lambda/scrape-trigger && npm audit fix --prefix src/lambda/scoring-trigger && npm audit fix --prefix src/pipeline/places-task && npm audit fix --prefix src/pipeline/scrape-task && npm audit fix --prefix src/pipeline/scoring-task",
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "dupes": "jscpd src lib",
    "todos": "leasot 'src/api/src/**/*.{ts,tsx,js,jsx}' 'src/api/prisma/**/*.ts' 'src/nextjs-web/src/**/*.{ts,tsx,js,jsx}' 'src/web-admin-spa/src/**/*.{ts,tsx,js,jsx}' 'src/lead-gen-spa/src/**/*.{ts,tsx,js,jsx}' 'lib/**/*.ts' 'bin/**/*.ts' 'test/**/*.ts' --reporter table",
    "keywords": "node scripts/find-keywords.js"
  },
  "devDependencies": {
    "@types/jest": "^30",
    "@types/node": "^24.10.1",
    "aws-cdk": "2.1104.0",
    "jest": "^30",
    "jscpd": "^4.0.6",
    "leasot": "^14.4.0",
    "ts-jest": "^29",
    "ts-node": "^10.9.2",
    "typescript": "~5.9.3"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.236.0",
    "cdk-fck-nat": "^1.6.22",
    "constructs": "^10.0.0",
    "token-injectable-docker-builder": "^1.11.0"
  }
}
