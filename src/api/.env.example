# Database (use 'db' hostname when running in Docker, 'localhost' otherwise)
DATABASE_URL=postgresql://postgres:postgres@db:5432/fca_db

# Server
PORT=3000
NODE_ENV=development

# Logging
LOG_LEVEL=info
